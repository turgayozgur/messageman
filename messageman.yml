# mode: sidecar # sidecar, gateway
# metric: 
#   enabled: true
#   exporter: prometheus
# rabbitmq:
#   url: "amqp://guest:guest@localhost:5672/"
# services:
#   - name: servicex
#     url: http://localhost:81
#     readiness:
#       path: /readiness
#     workers:
#       - path: /api/email/send
#         queue: email

mode: gateway # sidecar, gateway
metric: 
  enabled: true
  exporter: prometheus
rabbitmq:
  url: amqp://guest:guest@localhost:5672/
services:
  - name: workerapi
    url: http://workerapi:81 # do not use http:// for gRPC. Also, the example's grpc port is 83 and the REST one is 81
    #type: gRPC
    workers:
      - queue: email
        path: /api/email/send # not required for gRPC services.
